
/*
  auth-firebase.js
  Paste your Firebase config below and uncomment the firebase initialization.
  This file handles:
   - Firebase Auth sign-in
   - Redirect to index.html after login
   - (Later) you can pull user role from /users/{uid}.role in Realtime DB or Firestore
*/

console.log('Place your Firebase config here and enable Auth + Database in Firebase Console.');

// Example:
// const firebaseConfig = {...};
// firebase.initializeApp(firebaseConfig);
// const auth = firebase.auth();

// document.getElementById('btnLogin').addEventListener('click', async ()=>{
//   const email = document.getElementById('email').value;
//   const pass = document.getElementById('password').value;
//   try{
//     const res = await auth.signInWithEmailAndPassword(email, pass);
//     // after login redirect
//     window.location.href = 'index.html';
//   }catch(e){
//     alert(e.message);
//   }
//});
